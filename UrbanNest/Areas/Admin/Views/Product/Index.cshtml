@model List<Product>

<div class="container">
    <div class="card-header row pt-4 pb-3">
        <div class="col-md-6">
            <h2 class="text-dark" style="font-size: 1.25rem;">
                Product List
            </h2>
        </div>
        <div class="col-md-6 text-md-end text-center">
            <a asp-area="Admin" asp-controller="Product" asp-action="Upsert" class="btn btn-dark">
                <i class="bi bi-plus-circle"> </i> Create New Product
            </a>
        </div>
    </div>

    <table id="tblData" class="table table-bordered table-striped " width="100%">
        <thead>
            <tr>
                <th>Product Name</th>
                <th>Description</th>
                <th>Price</th>
                <th>Category</th>
                <th></th>
            </tr>
        </thead>
        @* <tbody> *@
            @* @foreach (var obj in Model.OrderBy(u => u.Price)) *@
            @* { *@
            @*     <tr> *@
            @*         <td>@obj.Title</td> *@
            @*         <td>@Html.Raw(obj.Description)</td> *@

            @*         <td>@obj.Price</td> *@
            @*         <td>@obj.Category.Name</td> *@

            @*         <td class="w-75 btn-group" role="group"> *@
            @*             <a asp-area="Admin" asp-controller="Product" asp-action="Upsert" asp-route-id="@obj.ID" class="btn btn-dark mx-2"> *@
            @*                 <i class="bi bi-pencil-square"></i> Edit *@
            @*             </a> *@
            @*             <a asp-area="Admin" asp-controller="Product" asp-action="Delete" asp-route-id="@obj.ID" class="btn btn-danger mx-2"> *@
            @*                 <i class="bi bi-trash-fill"></i> Delete *@
            @*             </a> *@
            @*         </td> *@
            @*     </tr> *@
            @* } *@
        @* </tbody> *@
    </table>
</div>

@section Scripts {
    <script src="~/js/Product.js"></script>
}
